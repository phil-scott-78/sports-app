# ESPN `core-athletes-id-events` — observed field guide

> Generated by `schema/tools/rollup.mjs` from 7 real responses across 7 leagues (1 sports: soccer). Everything below was OBSERVED in live data — nothing is guessed from documentation.

URL template(s) crawled:
- `sports.core.api.espn.com/v2/sports/soccer/athletes/…/events?…`

How to read the table:
- **presence** = % of responses (or parent objects) that carry the field. Low % usually means sport-specific or situational.
- **sports** = which sports the field was observed in (`all` = every sport crawled for this endpoint).
- `[]` in a path = array element; `{key}` = a dictionary whose keys are ids (play ids, atBat ids). `str-numeric` = string that always holds a number (ESPN serves scores as strings).
- **values** exhaustively lists small closed sets (enums) with observation counts; **e.g.** shows samples of open sets.

| path | type | presence | sports | values / examples |
|---|---|---|---|---|
| `count` | number | 100% | all | e.g. 0, 10 |
| `items` | array | 100% | all |  |
| `items[]` | object | — | all |  |
| `items[].$ref` | string | 100% | all | e.g. "http://sports.core.api.espn.com/v2/sports/soccer/leagues/esp.1/events/{id}?…", "http://sports.core.api.espn.com/v2/sports/soccer/leagues/club.friendly/events/{id}?…", "http://sports.core.api.espn.com/v2/sports/soccer/leagues/eng.1/events/{id}?…" |
| `pageCount` | number | 100% | all | e.g. 0, 1 |
| `pageIndex` | number | 100% | all | e.g. 0, 1 |
| `pageSize` | number | 100% | all | e.g. 25 |
