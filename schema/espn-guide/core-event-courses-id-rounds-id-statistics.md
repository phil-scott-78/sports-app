# ESPN `core-event-courses-id-rounds-id-statistics` — observed field guide

> Generated by `schema/tools/rollup.mjs` from 2 real responses across 2 leagues (1 sports: golf). Everything below was OBSERVED in live data — nothing is guessed from documentation.

URL template(s) crawled:
- `sports.core.api.espn.com/v2/sports/golf/leagues/lpga/events/…/courses/{id}/rounds/{id}/statistics?…`
- `sports.core.api.espn.com/v2/sports/golf/leagues/pga/events/…/courses/{id}/rounds/{id}/statistics?…`

How to read the table:
- **presence** = % of responses (or parent objects) that carry the field. Low % usually means sport-specific or situational.
- **sports** = which sports the field was observed in (`all` = every sport crawled for this endpoint).
- `[]` in a path = array element; `{key}` = a dictionary whose keys are ids (play ids, atBat ids). `str-numeric` = string that always holds a number (ESPN serves scores as strings).
- **values** exhaustively lists small closed sets (enums) with observation counts; **e.g.** shows samples of open sets.

| path | type | presence | sports | values / examples |
|---|---|---|---|---|
| `count` | number | 100% | all | e.g. 18 |
| `items` | array | 100% | all |  |
| `items[]` | object | — | all |  |
| `items[].courseId` | number | 100% | all | values: 33 (18), 76 (18) |
| `items[].holeId` | number | 100% | all | e.g. 31, 91, 151, 211 |
| `items[].holeNumber` | number | 100% | all | e.g. 1, 2, 3, 4 |
| `items[].holePar` | number | 100% | all | values: 4 (22), 3 (8), 5 (6) |
| `items[].holeStatistics` | array | 100% | all |  |
| `items[].holeStatistics[]` | object | — | all |  |
| `items[].holeStatistics[].abbreviation` | string | 100% | all | values: "+/- AVG" (36), "AVG SCORE" (36), "EAGLES" (36), "BIRDIES" (36), "PARS" (36), "BOGEYS" (36), "DOUBLES" (36), "OTHER" (36), "Rank" (36) |
| `items[].holeStatistics[].description` | string | 100% | all | values: "Score To Par" (36), "Average Score" (36), "Number of Eagles" (36), "Number of Birdies" (36), "Number of Pars" (36), "Number of Bogeys" (36), "Number of Double Bogeys" (36), "Other" (36), "Rank" (36) |
| `items[].holeStatistics[].displayName` | string | 100% | all | values: "Score To Par" (36), "Average Score" (36), "Eagles" (36), "Birdies" (36), "Pars" (36), "Bogeys" (36), "Double Bogeys" (36), "Other" (36), "Rank" (36) |
| `items[].holeStatistics[].displayValue` | str-numeric | 100% | all | e.g. "1", "0", "2", "5" |
| `items[].holeStatistics[].name` | string | 100% | all | values: "scoreToPar" (36), "avgScore" (36), "eagles" (36), "birdies" (36), "pars" (36), "bogeys" (36), "dBogey" (36), "dBogeyPlus" (36), "rank" (36) |
| `items[].holeStatistics[].value` | number | 100% | all | e.g. 1, 0, 2, 5 |
| `items[].holeYards` | number | 100% | all | e.g. 412, 434, 341, 440 |
| `items[].round` | number | 100% | all | values: 0 (36) |
| `pageCount` | number | 100% | all | e.g. 1 |
| `pageIndex` | number | 100% | all | e.g. 1 |
| `pageSize` | number | 100% | all | e.g. 25 |
