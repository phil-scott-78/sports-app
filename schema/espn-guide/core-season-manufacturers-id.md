# ESPN `core-season-manufacturers-id` — observed field guide

> Generated by `schema/tools/rollup.mjs` from 1 real responses across 1 leagues (1 sports: racing). Everything below was OBSERVED in live data — nothing is guessed from documentation.

URL template(s) crawled:
- `sports.core.api.espn.com/v2/sports/racing/leagues/f1/seasons/…/manufacturers/{id}?…`

How to read the table:
- **presence** = % of responses (or parent objects) that carry the field. Low % usually means sport-specific or situational.
- **sports** = which sports the field was observed in (`all` = every sport crawled for this endpoint).
- `[]` in a path = array element; `{key}` = a dictionary whose keys are ids (play ids, atBat ids). `str-numeric` = string that always holds a number (ESPN serves scores as strings).
- **values** exhaustively lists small closed sets (enums) with observation counts; **e.g.** shows samples of open sets.

| path | type | presence | sports | values / examples |
|---|---|---|---|---|
| `$ref` | string | 100% | all | e.g. "http://sports.core.api.espn.com/v2/sports/racing/leagues/f1/seasons/{id}/manufacturers/{id}?…" |
| `abbreviation` | string | 100% | all | e.g. "DH" |
| `color` | string | 100% | all | e.g. "FF8700" |
| `displayName` | string | 100% | all | e.g. "McLaren" |
| `eventLog` | object | 100% | all |  |
| `eventLog.$ref` | string | 100% | all | e.g. "http://sports.core.api.espn.com/v2/sports/racing/leagues/f1/seasons/{id}/manufacturers/{id}/eventlog?…" |
| `id` | str-numeric | 100% | all | e.g. "106892" |
| `name` | string | 100% | all | e.g. "McLaren" |
| `shortDisplayName` | string | 100% | all | e.g. "McLaren" |
