# ESPN `core-competition-momentum` — observed field guide

> Generated by `schema/tools/rollup.mjs` from 1 real responses across 1 leagues (1 sports: soccer). Everything below was OBSERVED in live data — nothing is guessed from documentation.

URL template(s) crawled:
- `sports.core.api.espn.com/v2/sports/soccer/leagues/fifa.world/events/…/competitions/{id}/momentum?…`

How to read the table:
- **presence** = % of responses (or parent objects) that carry the field. Low % usually means sport-specific or situational.
- **sports** = which sports the field was observed in (`all` = every sport crawled for this endpoint).
- `[]` in a path = array element; `{key}` = a dictionary whose keys are ids (play ids, atBat ids). `str-numeric` = string that always holds a number (ESPN serves scores as strings).
- **values** exhaustively lists small closed sets (enums) with observation counts; **e.g.** shows samples of open sets.

| path | type | presence | sports | values / examples |
|---|---|---|---|---|
| `count` | number | 100% | all | e.g. 109 |
| `items` | array | 100% | all |  |
| `items[]` | object | — | all |  |
| `items[].addedClock` | number | 100% | all | values: 0 (25) |
| `items[].clock` | number | 100% | all | e.g. 60, 120, 180, 240 |
| `items[].competitor` | object | 100% | all |  |
| `items[].competitor.$ref` | string | 100% | all | e.g. "http://sports.core.api.espn.com/v2/sports/soccer/leagues/fifa.world/events/{id}/competitions/{id}/competitors/{id}?…" |
| `items[].displayClock` | string | 100% | all | e.g. "1'", "2'", "3'", "4'" |
| `items[].period` | number | 100% | all | values: 1 (25) |
| `items[].probability` | number | 100% | all | e.g. 0.00452, 0.00925, 0.00851, 0.00404 |
| `pageCount` | number | 100% | all | e.g. 5 |
| `pageIndex` | number | 100% | all | e.g. 1 |
| `pageSize` | number | 100% | all | e.g. 25 |
