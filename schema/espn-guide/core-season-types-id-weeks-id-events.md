# ESPN `core-season-types-id-weeks-id-events` — observed field guide

> Generated by `schema/tools/rollup.mjs` from 1 real responses across 1 leagues (1 sports: football). Everything below was OBSERVED in live data — nothing is guessed from documentation.

URL template(s) crawled:
- `sports.core.api.espn.com/v2/sports/football/leagues/nfl/seasons/…/types/{id}/weeks/{id}/events?…`

How to read the table:
- **presence** = % of responses (or parent objects) that carry the field. Low % usually means sport-specific or situational.
- **sports** = which sports the field was observed in (`all` = every sport crawled for this endpoint).
- `[]` in a path = array element; `{key}` = a dictionary whose keys are ids (play ids, atBat ids). `str-numeric` = string that always holds a number (ESPN serves scores as strings).
- **values** exhaustively lists small closed sets (enums) with observation counts; **e.g.** shows samples of open sets.

| path | type | presence | sports | values / examples |
|---|---|---|---|---|
| `$meta` | object | 100% | all |  |
| `$meta.parameters` | object | 100% | all |  |
| `$meta.parameters.season` | array | 100% | all |  |
| `$meta.parameters.season[]` | str-numeric | — | all | e.g. "2025" |
| `$meta.parameters.seasontypes` | array | 100% | all |  |
| `$meta.parameters.seasontypes[]` | str-numeric | — | all | e.g. "4" |
| `$meta.parameters.week` | array | 100% | all |  |
| `$meta.parameters.week[]` | str-numeric | — | all | e.g. "1" |
| `count` | number | 100% | all | e.g. 0 |
| `items` | array | 100% | all |  |
| `items[]` |  | — |  |  |
| `pageCount` | number | 100% | all | e.g. 0 |
| `pageIndex` | number | 100% | all | e.g. 0 |
| `pageSize` | number | 100% | all | e.g. 25 |
