# ESPN `core-competition-manufacturers-id-statistics-id` — observed field guide

> Generated by `schema/tools/rollup.mjs` from 1 real responses across 1 leagues (1 sports: racing). Everything below was OBSERVED in live data — nothing is guessed from documentation.

URL template(s) crawled:
- `sports.core.api.espn.com/v2/sports/racing/leagues/f1/events/…/competitions/{id}/manufacturers/{id}/statistics/{id}?…`

How to read the table:
- **presence** = % of responses (or parent objects) that carry the field. Low % usually means sport-specific or situational.
- **sports** = which sports the field was observed in (`all` = every sport crawled for this endpoint).
- `[]` in a path = array element; `{key}` = a dictionary whose keys are ids (play ids, atBat ids). `str-numeric` = string that always holds a number (ESPN serves scores as strings).
- **values** exhaustively lists small closed sets (enums) with observation counts; **e.g.** shows samples of open sets.

| path | type | presence | sports | values / examples |
|---|---|---|---|---|
| `$ref` | string | 100% | all | e.g. "http://sports.core.api.espn.com/v2/sports/racing/leagues/f1/events/{id}/competitions/{id}/manufacturers/{id}/statistics/{id}?…" |
| `competition` | object | 100% | all |  |
| `competition.$ref` | string | 100% | all | e.g. "http://sports.core.api.espn.com/v2/sports/racing/leagues/f1/events/{id}/competitions/{id}?…" |
| `manufacturer` | object | 100% | all |  |
| `manufacturer.$ref` | string | 100% | all | e.g. "http://sports.core.api.espn.com/v2/sports/racing/leagues/f1/seasons/{id}/manufacturers/{id}?…" |
| `splits` | object | 100% | all |  |
| `splits.abbreviation` | string | 100% | all | e.g. "TOT" |
| `splits.categories` | array | 100% | all |  |
| `splits.categories[]` | object | — | all |  |
| `splits.categories[].abbreviation` | string | 100% | all | e.g. "gen" |
| `splits.categories[].displayName` | string | 100% | all | e.g. "General" |
| `splits.categories[].name` | string | 100% | all | e.g. "general" |
| `splits.categories[].shortDisplayName` | string | 100% | all | e.g. "General" |
| `splits.categories[].stats` | array | 100% | all |  |
| `splits.categories[].stats[]` | object | — | all |  |
| `splits.categories[].stats[].abbreviation` | string | 100% | all | e.g. "WINS", "T5", "T10", "PTS" |
| `splits.categories[].stats[].description` | string | 100% | all | e.g. "Wins", "Top 5", "Top 10", "Points" |
| `splits.categories[].stats[].displayName` | string | 100% | all | e.g. "Wins", "Top 5", "Top 10", "Points" |
| `splits.categories[].stats[].displayValue` | str-numeric | 100% | all | e.g. "1", "2", "27" |
| `splits.categories[].stats[].name` | string | 100% | all | e.g. "wins", "top5", "top10", "points" |
| `splits.categories[].stats[].shortDisplayName` | string | 100% | all | e.g. "Wins", "Top 5", "Top 10", "Points" |
| `splits.categories[].stats[].value` | number | 100% | all | e.g. 1, 2, 27 |
| `splits.categories[].summary` | string | 100% | all | e.g. "" |
| `splits.id` | str-numeric | 100% | all | e.g. "0" |
| `splits.name` | string | 100% | all | e.g. "All Splits" |
| `splits.type` | string | 100% | all | e.g. "total" |
